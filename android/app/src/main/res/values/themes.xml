<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Must be an AppCompat-derived theme: MainActivity is an
         AppCompatActivity (required for per-app locale switching), and it
         throws at runtime if given a plain platform theme instead. -->
    <style name="Theme.InventoryApp" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>
